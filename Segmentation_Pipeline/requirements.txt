# ── Web framework ────────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── Computer vision ───────────────────────────────────────────────────────────
ultralytics>=8.3.237
# Headless build avoids numpy<2 conflicts on macOS
opencv-python-headless==4.10.0.84
Pillow>=10.0.0
# SAM3 requires numpy<2
"numpy>=1.26,<2"

# ── PyTorch ───────────────────────────────────────────────────────────────────
# macOS / CPU: already handled below in install steps
# CUDA (Linux): install manually BEFORE this file:
#   pip install torch==2.10.0 torchvision --index-url https://download.pytorch.org/whl/cu128

# ── SAM3 ─────────────────────────────────────────────────────────────────────
# Install from source — see README step 3:
#   pip install git+https://github.com/facebookresearch/sam3.git

# ── HuggingFace (SAM3 checkpoint download) ────────────────────────────────────
huggingface_hub>=0.24.0
