# Homography Trapezoidal

Flatten an image using four reference points, or provide camera orentation details (height, pitch, FOV)

```bash
# Setup
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Run
python app.py
```